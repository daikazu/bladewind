<?php

declare(strict_types=1);

/**
 * The class half of a Livewire multi-file component resolved through Finder's
 * `multiFileAsIndex` path (`<location>/panel/index`). Never autoloaded.
 */
class PanelIndex
{
    public int $count = 0;
}
