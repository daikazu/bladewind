{
    "name": "bladewind-tachyons-fixture",
    "private": true,
    "description": "Regenerates tests/fixtures/public-tachyons from the Tachyons npm package, minified the way a Vite build would ship it. The output is committed; the suite never runs this.",
    "scripts": {
        "build": "node build.mjs"
    },
    "devDependencies": {
        "tachyons": "^4.12"
    }
}
