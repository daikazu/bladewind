{
    "name": "bladewind-tailwind3-fixture",
    "private": true,
    "description": "Regenerates tests/fixtures/public-tailwind3 with the real Tailwind CSS 3 compiler. The output is committed; the suite never runs this.",
    "scripts": {
        "build": "node build.mjs"
    },
    "devDependencies": {
        "lightningcss": "^1.33.0",
        "postcss": "^8.4",
        "tailwindcss": "^3.4"
    }
}
